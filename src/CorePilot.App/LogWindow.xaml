<Window x:Class="CorePilot.App.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CorePilot Activity Log"
        Width="1050" Height="650" MinWidth="760" MinHeight="460"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="160"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Activity Log" FontSize="24" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding CurrentStatus}"
                           Foreground="{StaticResource MutedBrush}"
                           TextWrapping="Wrap" Margin="0,4,0,0"/>
                <TextBlock Text="{Binding LogFilePath}"
                           Foreground="{StaticResource MutedBrush}"
                           FontSize="11" TextWrapping="Wrap" Margin="0,3,0,0"/>
            </StackPanel>

            <WrapPanel Grid.Column="1" VerticalAlignment="Top">
                <Button Content="Open folder" Click="OpenFolder_OnClick"/>
                <Button Content="Copy path" Click="CopyPath_OnClick"/>
                <Button Content="Clear view" Click="ClearView_OnClick"/>
            </WrapPanel>
        </Grid>

        <ListView x:Name="LogList"
                  Grid.Row="2"
                  ItemsSource="{Binding Entries}"
                  SelectionMode="Single"
                  Background="#12151A"
                  BorderBrush="{StaticResource BorderBrush}"
                  BorderThickness="1"
                  Foreground="{StaticResource TextBrush}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Time" Width="105" DisplayMemberBinding="{Binding TimeText}"/>
                    <GridViewColumn Header="Level" Width="90" DisplayMemberBinding="{Binding LevelText}"/>
                    <GridViewColumn Header="Area" Width="140" DisplayMemberBinding="{Binding Area}"/>
                    <GridViewColumn Header="Message" Width="610" DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Border Grid.Row="4"
                Background="#12151A"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Selected entry detail / stack trace"
                           FontWeight="SemiBold"/>
                <TextBox Grid.Row="1"
                         Margin="0,8,0,0"
                         Text="{Binding SelectedItem.Detail, ElementName=LogList}"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Background="Transparent"
                         Foreground="{StaticResource TextBrush}"
                         BorderThickness="0"
                         FontFamily="Consolas"/>
            </Grid>
        </Border>
    </Grid>
</Window>
